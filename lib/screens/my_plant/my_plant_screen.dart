import 'package:flutter/material.dart';

class MyPlantScreen extends StatelessWidget {
  const MyPlantScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child:Text("MyPlantScreen"),
      ),
    );
  }
}
